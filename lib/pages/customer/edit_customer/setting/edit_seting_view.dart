import 'package:flutter/material.dart';

class EditSettingView extends StatelessWidget {
  const EditSettingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit'),
    );
  }
}
